import styled from '@emotion/styled'
import MuiGrid from '@mui/material/Grid'
import StyledOverflowContainer from '../OverflowContainer'

const StyledGrid = styled(StyledOverflowContainer)``.withComponent(MuiGrid)

export default StyledGrid
